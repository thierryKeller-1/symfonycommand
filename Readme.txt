
env variables:
    SYMFONY_PROJECT_FOLDER_PATH="path to symfony project directory"
    COMMAND_PROJECT_FOLDER_PATH="path to this repository directory"
    LOG_FOLDER_PATH="path to directory where log file will be stored"
    START_DATE="date debut 'yyyy-mm-dd"
    END_DATE="date fin 'yyyy-mm-dd"

to run this project:

    1 - Ensure you have python3 installed in your machine, 
        if not run "python3 -m pip install -r requirements.txt or pip install -r requirements.txt"
    2 - go out of this project directory and run 
        if windows "py symfonie --help to show all commands
        if linux "python3 symfonie --help to shwow all commands



